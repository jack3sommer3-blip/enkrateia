<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" role="img" aria-label="007 badge">
  <!-- single outer circle -->
  <circle cx="256" cy="256" r="210" fill="none" stroke="currentColor" stroke-width="12" opacity="0.55"/>

  <!-- 0 0 7 mark (thin, modern, no font dependency) -->
  <!-- two zeros -->
  <circle cx="206" cy="256" r="44" fill="none" stroke="currentColor" stroke-width="14" stroke-linecap="round" stroke-linejoin="round" opacity="0.95"/>
  <circle cx="298" cy="256" r="44" fill="none" stroke="currentColor" stroke-width="14" stroke-linecap="round" stroke-linejoin="round" opacity="0.95"/>

  <!-- seven (to the right, no overlap with second 0) -->
  <path d="M348 220 H418 L372 318"
        fill="none"
        stroke="currentColor"
        stroke-width="14"
        stroke-linecap="round"
        stroke-linejoin="round"
        opacity="0.95"/>
</svg>
